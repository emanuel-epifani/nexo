import {nexo} from "../nexo";
import { describe, it, expect } from 'vitest';


describe.skip('Manual Debug', () => {
  it('should pass', () => {
    // nexo.kv.;

    expect(true).toBe(true);
  });
});