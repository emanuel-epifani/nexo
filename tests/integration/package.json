{
  "name": "nexo-integration-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Integration tests for Nexo Broker using Vitest",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@nexo/client": "file:../../sdk/ts",
    "tsx": "^4.7.0",
    "vitest": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
